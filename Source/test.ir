; ModuleID = 'clear'
source_filename = "clear"

define void @main() {
entry:
}

define void @hello(i32 %type) {
entry:
  %type1 = alloca i32, align 4
  store i32 %type, ptr %type1, align 4
  %h = alloca double, align 8
  store double 2.000000e+03, ptr %h, align 8
  ret void
}

define void @another_function(float %var, double %another_var) {
entry:
  %var1 = alloca float, align 4
  store float %var, ptr %var1, align 4
  %another_var2 = alloca double, align 8
  store double %another_var, ptr %another_var2, align 8
  %hello = alloca i32, align 4
  store i64 5, ptr %hello, align 4
  ret void
}
